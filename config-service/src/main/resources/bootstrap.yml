
server:
  port: 8081

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/DeveloperATT23/attapp-shopping.git
          searchPaths: config-data
          username: ${GIT_USER}
          password: ${GIT_PASSWORD}
  security:
    user:
      name: root
      password: s3cr3t

        # uri: https://github.com/digitallab-academy/ms-course-youtube.git
        # uri: https://github.com/arcitec2302/ms-course-youtube.git
