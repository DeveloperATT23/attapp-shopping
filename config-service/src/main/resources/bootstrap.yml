server:
  port: 8081

spring:
  cloud:
    config:
      server:
        git:
          url: https://github.com/arcitec2302/microservicesWithSpringBoot.git
          searchPath: config-data
          username: ${GIT_USER}
          password: ${GIT_PASSWD}

  security:
    user: root
    password: s3cre3t